Use when the user asks to search, inspect, summarize, or answer from the personal knowledge base or ingested documents.
Required param: `query`.
Optional params: `document_limit`, `chunks_per_document`.
Prefer this over guessing from memory when the answer should come from PKB documents such as invoices, receipts, contracts, reports, uploaded files, or ingested web pages.
