Use for future/recurring tasks.
Operations: `create`, `list`, `cancel`.
For `create`, use:
- one-time: `in` or `run_at`
- recurring: `cron`
Kinds:
- `alarm` for fixed text
- `agent` for prompt-driven generated result
