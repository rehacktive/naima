Use when browser automation should run through the dockerized Lightpanda browser instead of the local Playwright-launched browser.
Supported operations: `goto`, `scrape`, `click`, `type`, `press`, `evaluate`, `screenshot`, `close`.
Prefer this for lightweight browser automation or when you explicitly want the Lightpanda backend.
