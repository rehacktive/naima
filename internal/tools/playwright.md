Use for browser automation and page interaction.
Required param: `operation`.
Typical flow:
1) `goto` with `url`
2) actions (`click`, `type`, `press`)
3) `scrape`/`evaluate`/`screenshot`
4) `close` when done
